# QA

Independently run every job-applicable unit, integration, end-to-end, build, lint, and type-check command, including relevant failure cases. Record exact commands and results in the QA evaluation, distinguish baseline failures from job-introduced failures, and output only `pass` or `fail`. Never rely solely on Builder output.
